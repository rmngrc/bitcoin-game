{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "sam build --base-dir .",
    "deploy": "sam deploy --parameter-overrides Stage=prod",
    "test": "vitest"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.398.0",
    "@aws-lambda-powertools/parser": "^2.14.0",
    "@aws-sdk/lib-dynamodb": "^3.398.0",
    "aws-lambda": "^1.0.7",
    "@middy/core": "^6.0.0",
    "@middy/error-logger": "^6.0.0",
    "@middy/http-error-handler": "^6.0.0",
    "uuid": "^11.0.5",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/node": "^22.13.1",
    "aws-sdk-client-mock": "^2.0.0",
    "esbuild": "^0.25.0",
    "vitest": "^3.0.5"
  }
}
